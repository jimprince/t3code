{
  "name": "@t3tools/oxlint-plugin-t3code",
  "private": true,
  "type": "module",
  "scripts": {
    "build": "tsc -p tsconfig.build.json",
    "typecheck": "tsc --noEmit",
    "test": "bun run build && vitest run --exclude 'dist/**'"
  },
  "dependencies": {
    "@effect/platform-node": "catalog:",
    "@oxlint/plugins": "^1.63.0",
    "effect": "catalog:"
  },
  "devDependencies": {
    "@effect/vitest": "catalog:",
    "@types/bun": "catalog:",
    "vitest": "catalog:"
  }
}
